import os 
import csv
filename=input("enter csv file name without extension := ")
headers=["name","age"]
data=[
    ["maty","23"],
    ["talo","21"]
]
with open(f"{filename}.csv",'w') as file:
    writer=csv.writer(file)
    writer.writerow(headers)
    writer.writerows(data)